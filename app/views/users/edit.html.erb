<h1>メッセージの編集</h1>

<%= render 'form' %>
<% @users.each do |user| %>
  <span class="label label-info"><%= user.name %></span>
    <%= user.body %>
    (<%= link_to "edit" , edit_user_path(current_user) %>)
  <br>

